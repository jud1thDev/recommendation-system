# 복사해서 .env 로 쓰거나 export 해서 주입한다. .env 는 커밋하지 않는다.

# Anthropic API 키 (필수). SDK가 환경변수에서 직접 읽는다.
ANTHROPIC_API_KEY=

# 사용할 모델 (선택, 기본 claude-opus-4-8)
LLM_MODEL=claude-opus-4-8

# 응답 최대 토큰 (선택, 기본 1024)
LLM_MAX_TOKENS=1024
