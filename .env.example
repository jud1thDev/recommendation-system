# 복사해서 .env 로 쓰거나 export 해서 주입한다. .env 는 커밋하지 않는다.

# OpenAI API 키 (필수). 앱이 환경변수 OPENAI_API_KEY 에서 읽는다.
OPENAI_API_KEY=

# 사용할 모델 (선택, 기본 gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# 응답 최대 토큰 (선택, 기본 1024)
OPENAI_MAX_TOKENS=1024

# (선택) 게이트웨이/호환 엔드포인트로 바꿀 때
# OPENAI_BASE_URL=https://api.openai.com/v1
